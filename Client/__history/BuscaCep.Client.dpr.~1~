program BuscaCep.Client;

uses
  System.StartUpCopy,
  FMX.Forms,
  BuscaCep.view in 'View\BuscaCep.view.pas' {Form2};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm2, Form2);
  Application.Run;
end.
